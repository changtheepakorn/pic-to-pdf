# คู่มือการติดตั้งและรันบน Home Server (MacBook M1 RAM 8GB / 256GB) 🍏🚀

เอกสารนี้สรุปขั้นตอนและคำแนะนำสำหรับการนำโปรเจกต์ **ไม่รกจอ Pic to PDF by Chang** ไปเปิดเป็นเซิร์ฟเวอร์ส่วนตัว (Home Server) บน MacBook M1 สำหรับใช้งานเองและให้อุปกรณ์ทุกเครื่องในบ้าน (iPhone, iPad, PC) เข้ามาใช้งานได้ตลอด 24 ชั่วโมง

---

## 1. ข้อมูลการใช้ทรัพยากรบน MacBook M1

| ทรัพยากร | สเปกเครื่อง | สิ่งที่โปรแกรมใช้จริง | ประสิทธิภาพ |
| :--- | :--- | :--- | :--- |
| **RAM** | 8 GB Unified | **~50 - 80 MB** (ไม่ถึง 1% ของ RAM) | สบายมาก ไม่หน่วง ไม่กินสเปก |
| **ความจุ (SSD)** | 256 GB | **~100 - 150 MB** (รวม Package) | ใช้พื้นที่น้อยมาก |
| **CPU** | Apple Silicon M1 | ประมวลผลภาพระดับมิลลิวินาที | ชิป M1 มี ARM NEON ช่วยเร่งความเร็วรูป |
| **การกินไฟ (Power)** | แบตเตอรี่ / Adapter | **~3 - 5 วัตต์** (โหมด Idle) | ประหยัดไฟมาก เปิดทิ้งไว้ 24 ชม. ค่าไฟไม่กี่สิบบาท/เดือน |

---

## 2. ขั้นตอนการติดตั้งบน Mac M1 (ทำครั้งเดียว)

### ขั้นตอนที่ 1: นำโฟลเดอร์โปรเจกต์ไปไว้ที่ Mac
คัดลอกโฟลเดอร์ `pic to pdf` ไปไว้ที่เครื่อง Mac (แนะนำวางไว้ที่ `~/Documents/pic to pdf` หรือหน้า Desktop)
> ⚠️ **ข้อสำคัญ:** **ไม่ต้องคัดลอกโฟลเดอร์ `.venv`** จากเครื่อง Windows ไปด้วย เพราะระบบจะสร้าง virtual environment ตัวใหม่ที่เป็น Native ARM64 สำหรับ Mac M1 ให้อัตโนมัติ

### ขั้นตอนที่ 2: ติดตั้ง `uv` (ตัวจัดการ Python ที่เร็วที่สุด)
เปิดแอป **Terminal** บน Mac แล้วคัดลอกคำสั่งนี้ไปวางแล้วกด Enter:
```bash
curl -LsSf https://astral.sh/uv/install.sh | sh
```
*(เมื่อติดตั้งเสร็จ ให้ปิด Terminal แล้วเปิดใหม่หนึ่งครั้ง หรือพิมพ์ `source $HOME/.cargo/env`)*

### ขั้นตอนที่ 3: เปิดสิทธิ์ไฟล์รัน (ทำครั้งแรกครั้งเดียว)
ใน Terminal เข้าไปที่โฟลเดอร์โปรเจกต์:
```bash
cd ~/Desktop/"pic to pdf"
chmod +x run.sh
```

---

## 3. วิธีเริ่มเปิดใช้งานเซิร์ฟเวอร์

### วิธีที่ 1: รันผ่านสคริปต์ `run.sh` (สะดวกที่สุด)
```bash
cd ~/Desktop/"pic to pdf"
./run.sh
```

### วิธีที่ 2: รันผ่านคำสั่ง `uv` โดยตรง
```bash
cd ~/Desktop/"pic to pdf"
uv run python app.py
```

เมื่อเซิร์ฟเวอร์เริ่มทำงาน หน้าต่าง Terminal จะแสดงผลลักษณะนี้:
```text
==================================================
 ไม่รกจอ Pic to PDF by Chang is running at:
   - บนเครื่องนี้:  http://localhost:5500/
   - ผ่านเครือข่าย: http://192.168.1.50:5500/  (เข้าจากมือถือ/iPad/คอมเครื่องอื่น)
 Output folder: /Users/.../pic to pdf/output
 Press Ctrl+C in this window to stop the server.
==================================================
```

---

## 4. การเข้าใช้งานจากอุปกรณ์ต่างๆ ในบ้าน

ตราบใดที่ MacBook M1 เปิดโปรแกรมนี้อยู่ อุปกรณ์ทุกเครื่องที่เชื่อมต่อ **Wi-Fi เดียวกันในบ้าน** สามารถเข้าใช้งานได้ทันที:

- **บนเครื่อง Mac เครื่องนั้นเอง**: เปิด Safari/Chrome ไปที่ `http://localhost:5500`
- **บน iPhone / iPad**: เปิด Safari พิมพ์ `http://192.168.1.xx:5500` (ตามเลข IP ที่ขึ้นใน Terminal)
- **บนคอมพิวเตอร์ Windows เครื่องอื่น**: เปิดเบราว์เซอร์พิมพ์ `http://192.168.1.xx:5500`

> 💡 **แนะนำสำหรับ iPhone/iPad:** เมื่อเปิดหน้าเว็บแล้ว ให้กดปุ่มแชร์ (Share) แล้วเลือก **"Add to Home Screen" (เพิ่มไปยังหน้าจอโฮม)** จะได้ไอคอนแอปบนมือถือ กดเข้าใช้งานได้เหมือนแอปแท้ๆ ทันที

---

## 5. คำแนะนำสำคัญสำหรับการเปิด MacBook M1 เป็น Home Server 24 ชม.

### 1. ป้องกันเครื่องหลับเมื่อพับหน้าจอ (Prevent Sleep on Clamshell)
ปกติถ้าพับจอ MacBook เครื่องจะเข้า Sleep ทันที:
- **วิธีที่ 1 (แนะนำและง่ายที่สุด):** ติดตั้งแอปฟรีจาก Mac App Store ชื่อ **[Amphetamine](https://apps.apple.com/app/amphetamine/id937984704)** แล้วเปิดสวิตช์ Indefinitely พร้อมติ๊กถูกที่ *Allow display sleep* (เพื่อให้จอดับประหยัดไฟแต่ระบบข้างในยังรันอยู่)
- **วิธีที่ 2 (ผ่าน Terminal):** พิมพ์คำสั่งสั่งห้ามหลับ:
  ```bash
  sudo pmset -a disablesleep 1
  ```
  *(ถ้าต้องการยกเลิกในภายหลัง ให้พิมพ์ `sudo pmset -a disablesleep 0`)*

### 2. การดูแลรักษาแบตเตอรี่ (Battery Longevity)
เนื่องจากต้องเสียบสายชาร์จทิ้งไว้เป็นเวลานาน:
- เปิดฟังก์ชัน **Optimized Battery Charging** ใน *System Settings > Battery*
- หรือแนะนำลงแอปฟรีชื่อ **AlDente** เพื่อล็อกการชาร์จแบตเตอรี่ไว้ที่ 70% - 80% ไม่ให้แบตเตอรี่บวมหรือเสื่อมสภาพ

### 3. แนะนำให้รันแบบ Native (ห้ามใช้ Docker Desktop บน RAM 8GB)
- โครงสร้างโปรเจกต์นี้เขียนด้วย Python 3 + `uv` ซึ่งรันเป็น Native ARM64 บน M1 ได้เต็มประสิทธิภาพ ใช้ RAM เพียง **~60 MB**
- หากนำไปรันบน Docker Desktop จะสูญเสีย RAM ไปฟรีๆ 2 - 3 GB ทำให้เครื่องเหลือ RAM ใช้งานน้อยลง

### 4. การตั้งค่าให้โปรแกรมเปิดตัวเองอัตโนมัติเมื่อเปิดเครื่อง (Auto-Start)
หากต้องการให้โปรแกรมรันอยู่เบื้องหลังตลอดเวลา แม้ปิดหน้าต่าง Terminal:

#### ใช้ `pm2` (แนะนำ ง่ายและเสถียรที่สุด):
```bash
# 1. ติดตั้ง pm2 (หากมี node ในเครื่อง)
npm install -g pm2

# 2. สั่งรัน app.py เป็น background service
cd ~/Desktop/"pic to pdf"
pm2 start "uv run python app.py" --name "pic-to-pdf"

# 3. บันทึกให้เปิดอัตโนมัติเมื่อรีสตาร์ทเครื่อง Mac
pm2 save
pm2 startup
```

หรือถ้ารันแบบง่ายๆ ไม่ติดตั้งอะไรเพิ่ม ใช้คำสั่ง `nohup`:
```bash
nohup uv run python app.py > server.log 2>&1 &
```

---

## 6. สรุปความพร้อมของระบบ
- [x] รองรับ Apple Silicon ARM64 100%
- [x] แสดงเลข IP ภายในบ้านอัตโนมัติ สะดวกต่อการพิมพ์บนมือถือ
- [x] สลับโหมด Light / Dark Mode ในตัว
- [x] สั่งพิมพ์ตรงผ่านเบราว์เซอร์ได้ทันที ไม่ต้องรอดาวน์โหลดไฟล์
- [x] จัดตารางรูปภาพ (1x1, 2x2, 2x3 ฯลฯ) และคลิกลากสลับลำดับรูปได้อิสระ
