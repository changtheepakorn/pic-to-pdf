# คู่มือการนำโปรเจกต์ขึ้น Cloud Hosting เพื่อใช้งานจริง ☁️🚀

เอกสารนี้สรุปตัวเลือก **Cloud Hosting ที่เปิดให้ใช้งานฟรี (Free Tier)** วิธีการนำขึ้น (Deploy) ทีละขั้นตอน และการเปรียบเทียบระหว่างการนำขึ้น Cloud กับการรันบน Mac M1 Home Server

---

## 1. มี Cloud หรือ Hosting ที่ไหนบ้าง ? ฟรีมั้ย ? 💰

สำหรับโปรแกรมภาษา **Python** ที่ต้องมีทั้งหน้าเว็บและประมวลผลแปลงภาพเป็น PDF ตัวเลือก Cloud ฟรีที่ดีที่สุดมีดังนี้ครับ:

| ผู้ให้บริการ Cloud | สเปก Free Tier | ค่าใช้จ่าย | ข้อดี | ข้อพิจารณา / ข้อจำกัด |
| :--- | :--- | :--- | :--- | :--- |
| **1. Render.com** *(แนะนำที่สุด)* | RAM 512 MB, CPU 0.1 vCPU | **ฟรี 100%** | เชื่อมต่อกับ GitHub ง่ายมาก, ออก HTTPS ให้อัตโนมัติ, ได้ชื่อเว็บ `xxxx.onrender.com` | ถ้าไม่มีคนใช้งานเกิน 15 นาที เครื่องจะหลับ (Sleep) เมื่อมีคนกดเข้าใหม่จะรอโหลด ~30-50 วินาทีในครั้งแรก |
| **2. Koyeb.com** | RAM 512 MB (Nano Service) | **ฟรี 100%** | รันผ่าน Dockerfile หรือ GitHub ได้เร็ว, ประสิทธิภาพดี | มีโควต้า Free Instance ให้ 1 ตัว |
| **3. Hugging Face Spaces** | RAM 16 GB, CPU 2 Cores | **ฟรี 100%** | สเปกสูงมาก (RAM 16GB), **เครื่องไม่หลับ (Always-on)** | เหมาะกับการรันผ่าน Docker template |
| **4. Railway.app** | RAM 512 MB - 8 GB | ฟรีเครดิต $5 แรก | ใช้ง่ายที่สุด เร็วที่สุด | หลังจากหมดเครดิตฟรี $5 จะต้องจ่ายรายเดือน ($5/เดือน) |

> ⚠️ **คำเตือนเกี่ยวกับ PythonAnywhere (ฟรี):** **ไม่แนะนำ** ให้ใช้ PythonAnywhere ในแพ็กเกจฟรี เพราะระบบของเขาจะบล็อกการดึงรูปภาพจากอินเทอร์เน็ตภายนอก (Whitelisted APIs only) ทำให้ฟังก์ชันโหลด URL รูปภาพใช้งานไม่ได้

---

## 2. ขั้นตอนการนำขึ้น Render.com (แนะนำสำหรับมือใหม่ ง่ายที่สุด 4 ขั้นตอน)

ทางเราได้เตรียมไฟล์ **`requirements.txt`**, **`Dockerfile`** และ **`render.yaml`** ไว้ในโฟลเดอร์นี้ให้พร้อมใช้งาน 100% แล้วครับ

### ขั้นตอนที่ 1: นำโค้ดขึ้น GitHub ของคุณ
1. ไปที่ [GitHub.com](https://github.com/) แล้วสร้าง Repository ใหม่ (ตั้งเป็น Public หรือ Private ก็ได้)
2. อัปโหลดไฟล์ในโฟลเดอร์นี้ขึ้นไปที่ GitHub Repository นั้น *(ไม่ต้องเอาโฟลเดอร์ `.venv` และ `output` ขึ้นไป)*

### ขั้นตอนที่ 2: สมัครและเชื่อมต่อ Render.com
1. ไปที่เว็บไซต์ [Render.com](https://render.com/) แล้วสมัครสมาชิก (สามารถกดล็อกอินด้วย GitHub ได้เลย)
2. เมื่อเข้าหน้า Dashboard ให้กดปุ่ม **"New +"** ที่มุมขวาบน แล้วเลือก **"Web Service"**
3. เลือก **"Build and deploy from a Git repository"** แล้วเลือก Repository โปรเจกต์ที่คุณเพิ่งอัปโหลดไป

### ขั้นตอนที่ 3: ตั้งค่าการ Deploy
กรอกข้อมูลเพียงไม่กี่ช่องดังนี้:
- **Name**: ตั้งชื่อเว็บตามชอบ (เช่น `pic-to-pdf-chang`)
- **Region**: เลือก `Singapore` (ใกล้ไทยที่สุด โหลดเร็วที่สุด)
- **Runtime**: เลือก `Python 3`
- **Build Command**: 
  ```bash
  pip install -r requirements.txt
  ```
- **Start Command**: 
  ```bash
  python app.py --no-browser
  ```
- **Instance Type**: เลือก **Free** ($0/month)

### ขั้นตอนที่ 4: กด Deploy และเริ่มใช้งาน
1. เลื่อนลงไปล่างสุดแล้วกดปุ่ม **"Create Web Service"**
2. ระบบจะเริ่ม Build และติดตั้งไลบรารี ใช้เวลาประมาณ 2-3 นาที
3. เมื่อเสร็จแล้ว จะขึ้นสถานะสีเขียวว่า **"Live"** พร้อม URL ประจำเว็บของคุณ เช่น:
   ```
   https://pic-to-pdf-chang.onrender.com
   ```
4. สามารถส่งลิงก์นี้ให้เพื่อน หรือเปิดใช้งานจากมือถือได้ทันทีตลอด 24 ชั่วโมง!

---

## 3. เปรียบเทียบ: นำขึ้น Cloud ฟรี VS รันบน Mac M1 Home Server

| หัวข้อเปรียบเทียบ | รันบน Cloud ฟรี (เช่น Render.com) | รันบน Mac M1 + Cloudflare Tunnel |
| :--- | :--- | :--- |
| **ค่าใช้จ่าย** | **ฟรี 100%** | **ฟรี 100%** (อาจมีค่าโดเมนเนมปีละ ~100-300 บาท ถ้าต้องการชื่อโดเมนส่วนตัว) |
| **การเปิดคอมทิ้งไว้** | **ไม่ต้องเปิดคอม** (รันบนเซิร์ฟเวอร์ต่างประเทศตลอดเวลา) | ต้องเปิดเครื่อง Mac เสียบปลั๊กทิ้งไว้ในบ้าน |
| **การหลับของระบบ (Cold Start)** | **มีหลับ** (ถ้าไม่มีคนใช้นานๆ เมื่อกดเข้ามาใหม่จะรอโหลด ~30-50 วินาที) | **ไม่มีหลับ** (กดปุ๊บ หน้าเว็บเด้งขึ้นทันที ไม่ต้องรอ) |
| **ความเร็วในการแปลงภาพ** | ปานกลาง (ตาม CPU ฟรี 0.1-0.2 Core) | **เร็วมากระดับเสี้ยววินาที** (ชิป M1 แรงกว่า Cloud ฟรีหลายเท่า) |
| **ความจุ RAM** | จำกัดที่ 512 MB | ได้เต็มๆ จาก RAM เครื่อง Mac (8 GB) |
| **ความเป็นส่วนตัวของไฟล์** | ไฟล์ภาพ/PDF ผ่านเซิร์ฟเวอร์ผู้ให้บริการ Cloud | ข้อมูลภาพทั้งหมดประมวลผลอยู่บนเครื่องของคุณเอง 100% |

---

## 4. สรุปคำแนะนำในการเลือกใช้งาน

- **เลือกใช้ Cloud ฟรี (Render.com) เมื่อ:**
  - ไม่อยากเปิดเครื่อง MacBook ทิ้งไว้ที่บ้าน
  - อยากได้ลิงก์ส่งให้เพื่อนหรือลูกค้าใช้ขำๆ หรือใช้งานทั่วไปที่ยอมรับการรอโหลดครั้งแรก 30 วินาทีได้
- **เลือกใช้ Mac M1 Home Server เมื่อ:**
  - ต้องการความเร็วสูงสุด กดปุ๊บมาปั๊บ
  - ใช้งานส่วนตัวบ่อยๆ ทั้งจากมือถือและคอมในบ้าน
  - ชอบความเป็นส่วนตัวของไฟล์งาน ไม่ต้องส่งข้อมูลไปประมวลผลบนเซิร์ฟเวอร์คนอื่น
